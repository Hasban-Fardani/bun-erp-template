---
id: F1.18
title: CRUD user + OpenAPI docs + CLI produktivitas
status: in_progress
depends_on: F1.7,F1.9,F1.15
---

# F1.18 — CRUD user + OpenAPI docs + CLI produktivitas

## Ruang lingkup

- `POST/DELETE /api/v1/users` (sebelumnya hanya list+get+patch) + test 8 kasus.
- `GET /api/openapi.json` (dibangun dari schema zod modul) + UI Scalar di `GET /api/docs`.
- CLI baru: `bun erp user:passwd <email> [sandi]` (reset + audit `user.password_reset`)
  dan `bun erp user:list`.
- Web: form tambah pengguna (Sheet), tombol hapus per baris, show-password di login,
  title/theme dari config `VITE_*`.

## Bukti (agent, menunggu verifikasi manusia)

- `bun test apps/server` -> 40 pass / 0 fail / 165 expect.
- `bun erp check` -> OK (6 gate: biome, tsc, skills, task, scope, slop).
- Login produksi terbukti 200 via curl setelah `user:passwd`.

## Cara verifikasi manusia (px0)

1. Buka <APP_URL> -> login sebagai owner.
2. Menu Pengguna -> "Tambah" -> isi form -> simpan -> baris muncul.
3. Hapus user uji -> baris hilang; cek menu Audit tercatat `user.created`/`user.deleted`.
4. Buka <API_URL>/api/docs -> review kontrak API.
5. `bun erp user:list` di repo -> daftar pengguna.
